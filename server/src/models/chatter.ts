export class chatter {
    private username: username
    constructor(username: username) {
        this.username = username
    }

    readMessage(chatRoom: string, messageId: number) {
        
    }
}